import React from 'react'

export default function Dashboard(){
  return (
    <div>
      <h1>Dashboard</h1>
      <div className="card">
        <p>Welcome to FlowBoard — this is a starter dashboard UI.</p>
        <p>Create workspaces, view tasks, and open a workspace board.</p>
      </div>
    </div>
  )
}
